minecraft {
    if (replaceGradleTokenInFile) {
        replaceIn "CodeChickenCorePlugin.java"
        if(gradleTokenVersion) {
            replace gradleTokenVersion, versionDetails().lastTag
        }
    }
}